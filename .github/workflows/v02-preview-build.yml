name: MuscleTrack v0.3.1 streak preview APK

on:
  workflow_dispatch:
  push:
    branches: [ feature/v0.2-muscle-health ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Apply workout-only UI and calendar
        run: python tool/apply_workout_only_ui.py

      - name: Apply streak rewards and avatar moods
        run: python tool/apply_rewards_v031.py

      - name: Create Android runner
        run: |
          chmod +x tool/bootstrap_android.sh
          ./tool/bootstrap_android.sh

      - name: Configure preview app
        run: |
          chmod +x tool/configure_preview_android.sh
          ./tool/configure_preview_android.sh

      - name: Analyze
        run: flutter analyze --no-fatal-infos --no-fatal-warnings

      - name: Test
        run: flutter test

      - name: Build preview APK
        run: flutter build apk --debug

      - uses: actions/upload-artifact@v4
        with:
          name: muscletrack-v031-streak-preview-apk
          path: build/app/outputs/flutter-apk/app-debug.apk
          retention-days: 30
